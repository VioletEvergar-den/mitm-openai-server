

# 命令行实现
- 使用cobra组织命令行参数，至少应该多个子命令
- 至少应该有api子命令，能够启动api服务
- api服务支持从命令行配置认证，如果用户没有配置的话，则不配置认证，表示没有认证，传递任何认证都能使用
- 对于每个子命令及其参数，请充分测试可用


# 前端界面要求
- 对于捕获到的请求，你应该有一个前端界面能够展示
- 这个前端界面应该有认证，比如固定的用户名和随机的密码，启动的时候把凭证信息打印到控制台上
- 用户应该登录之后才能看到界面，先是登录，登录成功能看到列表页
- 列表页就是捕获到的所有的请求数据 
- 除了列表页，还应该有一个界面，引导用户如何在可以配置openapi的地方，配置上当前这个服务
- 因为登录之后也有多个页面，所以应该有一个导航栏，导航栏上有不同的菜单，可以跳转到不同的页面

- 抓取到的内容可以存在到本地的单文件数据库中，你选择一个单文件数据库，那比如sqlite之类的
- 用户要能够在界面上配置转发到的openapi的服务器地址和凭证，或者选择使用内置的假的openapi服务，所以你应该在导航栏上加个新的菜单，单击跳转到配置页面
- 在配置页面上应该能看到当前保存的会话的位置和大小，用户可以手动单击清空当前存储的所有会话，或者用户也应该能够导出，导出的话就使用jsonline格式导出即可


# storage包的重构
- 把存储相关的逻辑抽取到pkg/storage下的单独包中
- 包括storage存储api的接口的定义，以及文件系统、sqlite两个类型的实现
- storage的接口的定义单独一个.go文件
- filesystem的实现单独一个.go文件，存储位置应该能够外部指定
- sqlite的实现单独一个.go文件，存储位置应该能够外部指定
- 对每个方法都写单元测试，把各种case测试得全一些
- 执行单元测试，保证每个单元测试都能跑通，并修复测试出来的一些bug
- 单元测试中不要使用mock，就直接在本地文件系统上存储和创建sqlite文件即可
- 相关的代码和方法，添加完善的中文注释


# 提交代码
- 提交本地所有变更，建议分为多次提交，尽可能多产生几次commit，每次只提交一点，这样commit多一些，也能更精细化一些；
- 每个commit消息规范一些，写清楚一些；
- 提交完推送到github仓库
- 注意不要使用阻塞式的git命令，尤其是git log、git diff等 


# utils的重构
- 把utils相关的代码，与具体业务无关的逻辑，重构抽取到pkg/utils包下面
- 对每个方法都写单元测试，把各种case测试得全一些
- 执行单元测试，保证每个单元测试都能跑通，并修复测试出来的一些bug
- 单元测试中不要使用mock，就直接在本地文件系统上存储和创建sqlite文件即可
- 相关的代码和方法，添加完善的中文注释


# openai api逻辑的重构
- 把openai api相关的逻辑都抽取到pkg/openai包下面
- 包括我们内置的一个假的只返回随机数据的openai的实现，放到一个单独的.go文件中，这里应该留出后续的扩展的接口，因为后面可能会可以让用户指定，根据不同的匹配条件返回用户指定的响应
- 包括转发到指定openai服务器的逻辑，单独的放到一个单独的.go文件中，注意这里应该是能够指定参数，参数配置转发到的openai的服务器的url和认证凭证
- 因为这里涉及到两个openai服务的实现，所以最好是定义一个接口，接口放在单独的.go文件中，然后这两个openai服务作为这个接口的实现，这样代码整体看上去比较整洁
- 对每个方法都写单元测试，把各种case测试得全一些
- 执行单元测试，保证每个单元测试都能跑通，并修复测试出来的一些bug
- 单元测试中不要使用mock，就直接在本地文件系统上存储和创建sqlite文件即可
- 相关的代码和方法，添加完善的中文注释


# server逻辑的重构
- 把我们自己的api server相关的逻辑抽取到pkg/server下面，里面包括我们对外暴漏的api以及web界面等
- server包是一个比较高层的包，底下会调用其他包里暴漏出来的方法，你自己内存里有一些架构分层的概念 
- 对每个方法都写单元测试，把各种case测试得全一些
- 执行单元测试，保证每个单元测试都能跑通，并修复测试出来的一些bug
- 单元测试中不要使用mock，就直接在本地文件系统上存储和创建sqlite文件即可
- 相关的代码和方法，添加完善的中文注释





































































































































































































































































































































































































